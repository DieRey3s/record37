# record37

Front-end simple para Record37 — sitio con servicios de grabaciones deportivas.

Estructura mínima creada por la tarea de Notion `Frontend Record37`.

Cómo ver localmente:

```powershell
# abrir index.html en un navegador o usar un simple servidor:
python -m http.server 3000
# luego abrir http://localhost:3000/record37/
```

Próximos pasos sugeridos:
- Inicializar git y subir a GitHub en un repo llamado `record37`.
- Crear issues relacionadas (añadir imágenes reales, mejorar accesibilidad, añadir CI).


_Commit adicional para generar diferencia y permitir PR_